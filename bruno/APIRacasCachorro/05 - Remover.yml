info:
  name: 05 - Remover
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/racas/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
