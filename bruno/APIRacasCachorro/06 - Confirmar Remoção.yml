info:
  name: 06 - Confirmar Remoção
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/racas/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
